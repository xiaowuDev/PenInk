<Project>
  <PropertyGroup>
    <!-- Disable Avalonia build telemetry so sandboxed and CI builds never write outside the workspace. -->
    <UsedAvaloniaProducts></UsedAvaloniaProducts>
  </PropertyGroup>
</Project>
